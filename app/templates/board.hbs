
<div class="ui green segment">
  <span class="ui label">{{@model.employee.email}}</span>
  <button class="ui black button" type="button" {{action "logout"}}>
    Logout
  </button>
</div>

<h2>Préparation des commandes</h2>
<ul>
  {{#each @model.orders as |order|}}

    <li>
      <LinkTo @route="order" @model={{order.id}}>
        {{#if order.statusPrepared}}
          <i class="check green icon"></i>
        {{else}}
          <i style="color: orange" class="hourglass outline icon"></i>
        {{/if}}

        {{order.dateCreation}} <span class="ui circular label">{{order.count}} </span>
        {{#if order.toPay}}
          <label style="background-color: #1aa62a; color: white" class="ui rectangular label"> {{order.toPay}} </label>
        {{/if}}
      </LinkTo>
    </li>

  {{/each}}

  <h3>Produits préparés</h3>

  {{#each @model.orders as |order|}}

    {{#if order.isPrepared}}

      <i class="green check icon"></i>
      <LinkTo @route="order" @model={{order.id}}>
      {{order.dateCreation}} <span class="ui circular label">{{order.count}}</span>
      </LinkTo>
      <div class="ui green label">{{order.toPay}} euros</div>
      <br>

    {{/if}}

  {{/each}}
</ul>
